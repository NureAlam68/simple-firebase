

const Login = () => {

    

    const handleLogIn = () => {
      
    }

    return (
        <div>
            <button onClick={handleLogIn}>Login with Google</button>
        </div>
    );
};

export default Login;